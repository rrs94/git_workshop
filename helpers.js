const add = (number1, number2) => {
  return number1+number2;
}

const divide = (number1, number2) => {
  return number1/number2;
}

const multiply = (number1, number2) => {
  return number1*number2;
}

const substract = (number1, number2) => {
  return number1-number2; // this is a comment
}

// esto debe ser un test

module.exports = {
  add,
  divide,
  multiply,
  substract,
};
